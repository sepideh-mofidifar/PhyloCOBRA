from setuptools import setup, find_packages

setup(
    name="PhyloMICOM",  # The name of your package
    version="0.1.0",  # Version of your package
    author="Your Name",  # Your name or organization
    author_email="your.email@example.com",  # Your email
    description="A package for PhyloMICOM analysis",  # Short description of the package
    long_description=open('README.md').read(),  # Long description read from the README file
    long_description_content_type="text/markdown",  # Type of the README (markdown or reStructuredText)
    url="https://github.com/yourusername/PhyloMICOM",  # URL of the GitHub repository
    packages=find_packages(),  # Automatically find all the sub-packages (like micom, data, analysis)
    install_requires=[  # External dependencies (if any)
        "micom",  # Include micom if it's a dependency
        "pandas",  # Example, adjust based on your package's needs
        "numpy",  # Example, adjust based on your package's needs
    ],
    classifiers=[  # Optional: classifiers help users find your project
        "Programming Language :: Python :: 3",
        "License :: OSI Approved :: MIT License",
        "Operating System :: OS Independent",
    ],
    python_requires='>=3.6',  # Minimum Python version
)
